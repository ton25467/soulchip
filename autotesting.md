# คู่มือระบบตรวจสอบอัตโนมัติ (Automated Testing Documentation)

เอกสารฉบับนี้อธิบายรายละเอียดเกี่ยวกับโครงสร้างการทดสอบระบบควบคุมอัตโนมัติ (Automated Unit Testing) ของโปรเจกต์เกม SoulChip ในระดับแกนหลัก (Core Logic) เพื่อตรวจสอบความถูกต้องของระบบการเดินทาง พิกัดกระเบื้องเฉลียง 2.5D ระบบกุญแจไขประตูล็อก การขึ้นลงบันไดสองทาง และการทิ้งสิ่งของจำลองตามสไตล์ Resident Evil 0

> [!IMPORTANT]
> **นโยบายการทดสอบ (Testing Policy)**
> ชุดการตรวจสอบอัตโนมัติในเอกสารนี้จะต้องถูกรันตรวจทาน (`go test ./...`) ทุกครั้งในกรณีดังต่อไปนี้:
> 1. **เมื่อมีการอัปเดตระบบใหม่ (System Updates & Additions)**: เพื่อป้องกันการเกิดบั๊กถอยหลัง (Regression) บนฟังก์ชันแกนหลักเดิมของเกม
> 2. **เมื่อมีการแก้ไขระบบเดิม (System Modifications)**: เพื่อยืนยันความเข้ากันได้และการสลับตัวแปรตรรกะว่ายังทำงานได้ตรงตามปกติ
> 3. **เมื่อต้องการตรวจสอบออบเจกต์และสภาพแวดล้อม (Object Integrity Check)**: เพื่อทดสอบว่าตัวละคร Gopher, ไอเทมบนแผนที่, ด่าน 1-4, ประตู และบันได มีปฏิสัมพันธ์โต้ตอบสอดคล้องตามตัวแปรพิกัดคณิตศาสตร์ทั้งหมด

> [!CAUTION]
> **ระบบแจ้งเตือนข้อผิดพลาดเข้า Backend ทันที (Instant Backend Error Alert)**
> หากชุดการทดสอบพบล้มเหลวหรือเกิดข้อผิดพลาดขึ้น **แม้แต่จุดเดียว (Exit Code != 0)** ฟังก์ชัน `TestMain` ในไฟล์ทดสอบจะส่งสัญญาณ HTTP POST แจ้งเตือนส่งพิกัดข้อผิดพลาดเข้าสู่ API `/report` ของระบบเซิร์ฟเวอร์จำลอง Backend เพื่อทำการแจ้งเตือนแบบด่วนสีแดง `🚨 [BACKEND ALERT]` บันทึกความล้มเหลวลงบน Console ทันที

---

## 🧪 โครงสร้างชุดทดสอบ (Unit Test Suite Overview)

ไฟล์ทดสอบหลักถูกเขียนรวบรวมไว้ที่ [internal/game/game_test.go](file:///f:/soulchip/internal/game/game_test.go) โดยประกอบด้วย 7 เคสทดสอบความสมบูรณ์ที่จำเป็นดังต่อไปนี้:

### 1. `TestPlayerCollision` (การเคลื่อนที่และการชนกำแพง)
*   **วัตถุประสงค์**: ยืนยันว่าขอบแผนที่และตารางกำแพง (`TileWall`) ทำหน้าที่บล็อกตัวละคร Gopher ได้ถูกต้อง
*   **การทดสอบ**: สร้างผู้เล่นที่ตำแหน่งเริ่มต้นและบังคับทิศทางชนกำแพงทึบ ระบบฟิสิกส์ต้องไม่อนุญาตให้พิกัดตัวละครเปลี่ยนแปลงเข้าสู่ช่องกำแพงนั้น

### 2. `TestItemCollection` (การเก็บสัมภาระอัตโนมัติ)
*   **วัตถุประสงค์**: ยืนยันว่าเมื่อผู้เล่นเดินทับกระเบื้องที่มีไอเทมวางอยู่ ระบบจะย้ายของลงสู่เป้ HUD ได้ทันที
*   **การทดสอบ**: วางกุญแจสีแดง (`ItemRedKey`) ที่พิกัด `(2,1)` และสั่งให้ผู้เล่นเดินไปทับพิกัดดังกล่าว ตรวจสอบธง `Collected = true` และจำนวนไอเทมในกระเป๋าเป้ผู้เล่นต้องเพิ่มขึ้น

### 3. `TestLockedDoors` (การเคลียร์ประตูล็อกแดง/น้ำเงิน)
*   **วัตถุประสงค์**: ยืนยันระบบความปลอดภัยของกุญแจเมื่อเดินชนประตูล็อก
*   **การทดสอบ**: บังคับเดินชนประตูแดงเมื่อไม่มีกุญแจแดง ระบบต้องบล็อกเหมือนกำแพงธรรมดา จากนั้นเพิ่มกุญแจแดงเข้ากระเป๋าและสั่งก้าวเหยียบซ้ำ ประตูแดง (`TileRedDoor`) ต้องเปลี่ยนสภาพเป็นพื้นว่าง (`TileEmpty`) และกุญแจถูกลบออกจากกระเป๋าเป้

### 4. `TestProceduralAlgorithms` (การสแกนผนังกั้นและสุ่ม $n^n$)
*   **วัตถุประสงค์**: ตรวจสอบความแม่นยำของอัลกอริทึมวิเคราะห์ขอบเขตห้อง (Partition Wall Scanner) และระบบทวีคูณจำนวนตามระดับด่าน
*   **การทดสอบ**:
    *   ป้อนข้อมูลตารางสี่เหลี่ยมที่มีผนังคั่นกลาง และยืนยันว่าอัลกอริทึมกรองได้ตำแหน่งผนังกึ่งกลางห้องออกมาได้อย่างปลอดภัย
    *   ตรวจสอบเกณฑ์สุ่มเกิด: ด่าน 1 ต้องได้กำแพงลวงตา 2 จุด, ด่าน 2 ได้ 4 จุด, ด่าน 3 ได้ 27 จุด ตามสมการ $n^n$

### 5. `TestSecretBossEncounter` (การปราบมินิบอส)
*   **วัตถุประสงค์**: ตรวจสอบการทำลายบอสม่วงลับและรับ Trophies
*   **การทดสอบ**: สั่งผู้เล่นเดินทับพิกัดบอสวิญญาณม่วง (`TileSecretBoss`) ยืนยันว่ากระเบื้องบอสถูกทลายออก และได้รับหัวกะโหลกเกียรติยศ (`ItemBossSkull`) เข้าสล็อตกระเป๋าเป้

### 6. `TestStaircaseClimbing` (ระบบขึ้นลงบันได RE0)
*   **วัตถุประสงค์**: ตรวจสอบว่ากระเบื้องบันไดสามารถเข้าเหยียบได้ปกติโดยไม่ขัดแย้งฟิสิกส์กำแพง และเข้าสู่การล็อกบังคับควบคุม
*   **การทดสอบ**: ยืนยันว่า `IsWall` บนบันไดคืนค่า `false` และเมื่อสั่ง `StartClimb` ตัวละครจะล็อกตัวแปร `isClimbing = true` พร้อมเริ่มเก็บรอบขยับแอนิเมชัน

### 7. `TestItemDrop` (ระบบทิ้งของบันทึกพิกัด RE0)
*   **วัตถุประสงค์**: ตรวจสอบคำสั่งสละสัมภาระลงเท้าและเดินกลับมาเก็บคืน
*   **การทดสอบ**:
    *   จำลองการสลัดกุญแจจากกระเป๋าลงตารางที่ยืน
    *   ยืนยันว่าไอเทมถูกสร้างขึ้นใหม่เป็นวัตถุไม่เก็บ (`Collected = false`) ตรงพิกัด Isometric ของด่านปัจจุบัน
    *   จำลองผู้เล่นเดินออกจากช่องดังกล่าวแล้วเดินย้อนกลับมาเหยียบซ้ำ ของต้องหายไปและกระเป๋าเป้ต้องได้รับกุญแจชิ้นเดิมกลับคืน 100%

---

## 💻 วิธีการรันคำสั่งทดสอบ (How to Run Tests)

คุณสามารถเปิด PowerShell หรือ Terminal ภายในโฟลเดอร์โปรเจกต์ `f:\soulchip` และใช้คำสั่งมาตรฐานของภาษา Go เพื่อสั่งรันการทดสอบทั้งหมดได้ดังนี้:

### คำสั่งมาตรฐาน (Standard Run):
```powershell
go test ./...
```

### คำสั่งดึงรายละเอียดอย่างถี่ถ้วน (Verbose Output):
```powershell
go test -v ./...
```

---

## 📈 ตัวอย่างผลลัพธ์การรันชุดทดสอบ (Sample Verification Output)

```text
?       github.com/soulchip/game        [no test files]
?       github.com/soulchip/game/assets [no test files]
=== RUN   TestPlayerCollision
--- PASS: TestPlayerCollision (0.00s)
=== RUN   TestItemCollection
--- PASS: TestItemCollection (0.00s)
=== RUN   TestLockedDoors
--- PASS: TestLockedDoors (0.00s)
=== RUN   TestProceduralAlgorithms
--- PASS: TestProceduralAlgorithms (0.00s)
=== RUN   TestSecretBossEncounter
--- PASS: TestSecretBossEncounter (0.00s)
=== RUN   TestStaircaseClimbing
--- PASS: TestStaircaseClimbing (0.00s)
=== RUN   TestItemDrop
--- PASS: TestItemDrop (0.00s)
PASS
ok      github.com/soulchip/game/internal/game   1.467s
```

> [!NOTE]
> ระบบตรวจสอบอัตโนมัติทั้งหมดทำงานบน **Go Standard Testing Library** ร่วมกับ Logic ของเกม โดยไม่ขึ้นกับ GPU หรือจอภาพภายนอก ทำให้สามารถรันบนระบบทดสอบแบบ CI/CD (Continuous Integration) ได้ทันทีอย่างมีเสถียรภาพสูงสุด
